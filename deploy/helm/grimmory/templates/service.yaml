apiVersion: v1
kind: Service
metadata:
  name: {{ include "tobyshelf.fullname" . }}
  labels:
    {{- include "tobyshelf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tobyshelf.selectorLabels" . | nindent 4 }}
